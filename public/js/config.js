// require.js configs
requirejs.config({
  // by default load any modules IDs from public/js/modules
  baseUrl:'./public/js/modules',
  // paths is relative to the baseUrl
  paths: {
    lib: './lib'
  }
});
